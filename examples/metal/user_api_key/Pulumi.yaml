name: equinix-metal-user_api_key
runtime: yaml
resources:
  # Create a new read-only user API key
  test:
    type: equinix:metal:UserApiKey
    properties:
      description: Read-only user key
      readOnly: true
